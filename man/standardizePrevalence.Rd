% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{standardizePrevalence}
\alias{standardizePrevalence}
\title{Direct Method Standardization of Prevalence Rates}
\usage{
standardizePrevalence(
  prevalenceData,
  referencePopulation,
  ageMin = NULL,
  ageMax = NULL,
  ageRightTruncation = NULL,
  ageCol = "age",
  genderCol = "gender",
  numeratorCol = "numerator",
  denominatorCol = "denominator"
)
}
\arguments{
\item{prevalenceData}{Data frame with stratified prevalence data.
Required columns: age, gender, numerator, denominator
(or outcome_count, population_count)}

\item{referencePopulation}{StandardizationReference object defining
the standard population for weighting}

\item{ageMin}{Numeric. Minimum age for filtering reference population.
If NULL (default), no lower bound applied.}

\item{ageMax}{Numeric. Maximum age for filtering reference population.
If NULL (default), no upper bound applied.}

\item{ageRightTruncation}{Numeric. Optional age threshold for collapsing
ages >= threshold into single "threshold+" group. Useful for handling
database age masking (e.g., Optum: 70+ all collapsed to "70+").
If NULL (default), no truncation applied.}

\item{ageCol}{Character. Name of age column in prevalenceData.
Default: "age"}

\item{genderCol}{Character. Name of gender column in prevalenceData.
Default: "gender"}

\item{numeratorCol}{Character. Name of numerator (case count) column.
Default: "numerator" (also accepts "outcome_count")}

\item{denominatorCol}{Character. Name of denominator (population) column.
Default: "denominator" (also accepts "population_count")}
}
\value{
List with elements:
\itemize{
\item standardized_rate: Numeric, standardized prevalence rate
\item ci_lower: Numeric, lower 95\% CI bound
\item ci_upper: Numeric, upper 95\% CI bound
\item crude_rate: Numeric, crude rate (all strata combined)
\item reference_name: Character, name of reference population
\item reference_year: Integer, year of reference
\item diagnostics: List with details (n_strata, total_numerator, total_denominator)
}
}
\description{
Applies direct method age-sex standardization to crude prevalence data
using a reference population. Supports demographic bounds matching and
age truncation for real-world database patterns (e.g., Optum age masking).
}
\details{
Algorithm (direct method standardization):
\enumerate{
\item Filter reference population to demographic bounds (ageMin-ageMax)
\item If ageRightTruncation specified: collapse ages >= threshold into single group
\item Re-normalize weights within adjusted age groups
\item Join prevalence data to adjusted reference weights
\item Calculate crude rate per age-sex stratum: numerator/denominator
\item Multiply each stratum's crude rate by reference weight
\item Sum weighted rates = standardized rate
\item Calculate 95\% CI using Wilson score exact method
}

Output includes:
\itemize{
\item standardized_rate: Age-sex standardized prevalence rate
\item ci_lower, ci_upper: 95\% confidence interval (Wilson score exact)
\item crude_rate: Crude rate across all strata (for comparison)
\item reference_name, reference_year: Metadata
}
}
\examples{
\dontrun{
  # Simple example with prevalence data
  prev_data <- data.frame(
    age = c("0-4", "5-9", "10-14", "0-4", "5-9", "10-14"),
    gender = c(rep("Male", 3), rep("Female", 3)),
    numerator = c(10, 15, 20, 8, 12, 18),
    denominator = c(1000, 1000, 1000, 1000, 1000, 1000)
  )

  # Standardize using USA Census 2020
  data("usa_census_2020")
  result <- standardizePrevalence(
    prevalenceData = prev_data,
    referencePopulation = usa_census_2020
  )

  # Standardize with demographic bounds (18-75 age restriction)
  result <- standardizePrevalence(
    prevalenceData = prev_data,
    referencePopulation = usa_census_2020,
    ageMin = 18,
    ageMax = 75
  )

  # Handle Optum age masking (70+ collapsed)
  result <- standardizePrevalence(
    prevalenceData = prev_data,
    referencePopulation = usa_census_2020,
    ageRightTruncation = 70
  )
}

}
