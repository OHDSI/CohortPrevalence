% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R6ClassWrappers.R
\name{createTargetCohort}
\alias{createTargetCohort}
\title{Create a target cohort \code{TargetCohort} object}
\usage{
createTargetCohort(
  cohortId,
  cohortName,
  calculationMode = "era",
  circeJsonPath = NULL
)
}
\arguments{
\item{cohortId}{Integer: the cohort ID within the database results schema of interest.}

\item{cohortName}{Character string specifying a name for the cohort.}

\item{calculationMode}{Character: \code{"era"} (default, interval overlap) or \code{"occurrence"} (point-in-time).}

\item{circeJsonPath}{Character string path to the CIRCE JSON file. Required when
\code{calculationMode = "occurrence"}. Must follow one of two patterns:
\itemize{
\item ERA (Chronic): PrimaryCriteriaLimit='First', ExpressionLimit='First', no EndStrategy
\item OCCURRENCE (Code Events): PrimaryCriteriaLimit='All', ExpressionLimit='All', DateOffset EndStrategy
}}
}
\value{
A \code{TargetCohort} R6 object.
}
\description{
Constructs a \code{TargetCohort} object for use as the prevalence numerator or incidence target cohort.
When \code{calculationMode = "occurrence"}, a CIRCE JSON file is required and validated.
Non-CIRCE or derived cohorts can be used with the default \code{calculationMode = "era"}.
}
